{
  "class": "z2ui5_cl_ai_app_540",
  "sample": "sap.m.sample.DatePickerHidden",
  "entity": "sap.m.DatePicker",
  "file": "src/01/b05/z2ui5_cl_ai_app_540.clas.abap",
  "batch": "b05",
  "audit": {
    "frontend_action": true,
    "event_t_arg": true
  },
  "status": "generated",
  "checked": null,
  "deviations": [
    {
      "type": "IMPROVISED",
      "what": "DatePicker.openBy(domRef) is NOT in the CONTROL_METHODS whitelist (source-verified in app/webapp/core/FrontendAction.js); the port keeps the 1:1 wiring (press -> backend event -> follow_up_action control_by_id HiddenDP//openBy/<anchorId>), which the frontend rejects with a logged error until the whitelist grows - the picker does not open and the change toast cannot fire. Requested in pr/control-methods-openby-setactivepage (needs a new domRef-resolving arg kind; cites this port). No alternative exists: DatePicker has no public open() and no bindable open-state property (verified in sap/m/DatePicker.js)."
    },
    {
      "type": "POST_171",
      "what": "Button.ariaHasPopup (since UI5 1.84) is newer than 1.71 but kept for the 1:1 port on both Buttons - the app needs a UI5 release >= 1.84 to render it."
    },
    {
      "type": "POST_171",
      "what": "Link.ariaHasPopup (since UI5 1.86) is newer than 1.71 but kept for the 1:1 port - the app needs a UI5 release >= 1.86 to render it."
    },
    {
      "type": "POST_171",
      "what": "DatePicker.openBy(domRef) is NOT in the CONTROL_METHODS whitelist (source-verified in app/webapp/core/FrontendAction.js); the port keeps the 1:1 wiring (press -> backend event -> follow_up_action control_by_id HiddenDP//openBy/<anchorId>), which the frontend rejects with a logged error until the whitelist grows - the picker does not open and the change toast cannot fire. Requested in pr/control-methods-openby-setactivepage (needs a new domRef-resolving arg kind; cites this port). No alternative exists: DatePicker has no public open() and no bindable open-state property (verified in sap/m/DatePicker.js)."
    }
  ]
}
